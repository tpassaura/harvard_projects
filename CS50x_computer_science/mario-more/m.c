#include <cs50.h>
#include <stdio.h>

int main(void)
{
    //DEFINE VARIABLES
    int h;
    int i;
    int j;
    int k;
    int l;

    //GET HEIGHTH
    do
    {
        int number = get_int("HEIGHT?: ");
    }
    while (h < 1 || h > 8);

    int draw(number){
        draw(number - 1)
        printf(#)
    }

